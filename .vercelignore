# Archived, unwired copies of the removed in-app-breakout prelander. Kept in git as a rollback
# reference only — never deployed. These pages UA-sniff and redirect ad-review traffic elsewhere,
# which is exactly what gets a lander flagged, so they must not be reachable on the live domain.
justincase/

# Cloaking-technique demo/QA fixtures — reference only, never served on the live domain.
SIGNAT~1/

# Lander generators + the approved-page template. Build tooling, never served.
_lp-generator/

# Internal engineering docs and tooling. These were being SERVED: as of 2026-07-26
# https://www.tokrwd.co/.claude/skills/tokrwd-landers/SKILL.md returned 200, publishing the funnel
# architecture, the door domains and the spark-code scheme to anyone who guessed the path. Nothing
# on the site references .claude/, so excluding it cannot break a page.
.claude/

# TikTok server-to-server pipeline (README, importable n8n workflow, test script). These carry
# the Events API access token — ops tooling, NEVER served from the live domain.
tiktok-s2s/
